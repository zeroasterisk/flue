---
title: Getting Started
description: Set up a Flue project automatically or create your first agent manually.
lastReviewedAt: 2026-05-27
---

import CopyPrompt from '../../../components/docs/CopyPrompt.astro';

**Flue** is a TypeScript framework for building AI agents. Define your agents using the exact same harness-driven architecture used by Claude Code and other coding agents to build truely autonomous software. Write your agents with Flue and then run them anywhere (local CI, Node.js server, Cloudflare, etc.).

## Prerequisites

- **Node.js** — `>=22.18.0` minimum required version.
- **LLM** — At least one LLM model identifier to connect your agent to (ex: `anthropic/claude-sonnet-4-6`, `cloudflare/@cf/moonshotai/kimi-k2.6`)
- **LLM Provider** — API key(s) to connect to your favorite model provider. Cloudflare provides built-in `cloudflare/*` model access, no API keys required.
- **A coding agent (recommended)** — Several Flue features assume you have a coding agent available to run locally (Claude Code, Codex, etc.).
- **A container sandbox (optional)** — Flue includes a built-in virtual sandbox, suitible for many agentic workloads. If you need a real VM, consider a [container sandbox](/docs/ecosystem/overview/#sandbox-connectors).

## Quickstart

<CopyPrompt text="fetch https://flueframework.com/start.md to create a new agent" />

Copy this prompt and submit it to your coding agent in a new session. It will guide you through setting up an agent in a new or existing project, leaving you with a working Flue agent that you can run and talk to locally.

## Manual installation

***Note:** We recommend the [Quickstart](#quickstart) prompt above for guided setup and automatic validation. Use the steps below if you prefer to create your first agent by hand.*

### 1. Install Flue

In a new directory, install Flue and initialize your target. The `flue init` command creates `flue.config.ts`; you will add an agent module in the next step.

```bash
npm install @flue/runtime
npm install --save-dev @flue/cli
echo 'ANTHROPIC_API_KEY="your-api-key"' > .env
npx flue init --target node # or: --target cloudflare
```

Add `.env` to `.gitignore` and do not commit provider credentials. We use Anthropic in this example, but you can use any LLM provider that Pi supports. Read Pi's ["Providers" documentation](https://pi.dev/docs/latest/providers#api-keys) for the complete list of supported providers.


### 2. Create your first agent module

Create `agents/hello-world.ts`. This example uses Claude Sonnet, but you can choose any [supported model](https://pi.dev/models) and configure its provider credentials instead:

```ts title="agents/hello-world.ts"
import { createAgent } from '@flue/runtime';

export default createAgent(() => ({
  model: 'anthropic/claude-sonnet-4-6',
  instructions: 'Tell a funny "hello world" engineering joke.',
}));
```

Don't forget to export the `agent` from `agents/hello-world.ts`. This is what makes it available in Flue as `hello-world`.


### 3. Talk to your agent locally

For a Node.js target, open an interactive session with the discovered agent:

```bash
npx flue connect hello-world local --env .env
```

Once connected, type a message and press Enter. In this command, `hello-world` is the module name and `local` is the ID for this agent instance.

```text
$ npx flue connect hello-world local --env .env
[flue] Connected to hello-world/local. Enter a prompt per line; Ctrl-D to exit.
Hello! What can you help me with?
{
  "text": "I can help answer questions, draft content, summarize information, and more."
}
```

Congratulations! You have created your first Flue agent and opened a local conversation with it. From here, you can shape its behavior, add capabilities, or deploy it as part of an application.

## Next steps

- If you selected the Cloudflare target, continue with [Deploy on Cloudflare](/docs/ecosystem/deploy/cloudflare/) to configure and run your Worker.
- Learn how continuing interactions are modeled in [Agents](/docs/concepts/agents/).
- Create bounded operations around agents with [Workflows](/docs/guide/workflows/).
- Configure targets and local environment behavior in [Configuration](/docs/guide/configuration/).
- Choose execution capabilities in [Sandboxes](/docs/guide/sandboxes/).
